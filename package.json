{
  "name": "amazon-scraper",
  "version": "1.0.0",
  "description": "Amazon product scraper with Playwright stealth automation",
  "scripts": {
    "scrape": "node scripts/amazon-scrape.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
